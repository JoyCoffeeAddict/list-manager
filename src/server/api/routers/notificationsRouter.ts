// import * as z from "zod"

// import type { ListItem, PrismaClient, UserInorganization } from "@prisma/client"
// import { TRPCError } from "@trpc/server"
// import {
//   existingListItemSchema,
//   listSchema,
//   type listItemType,
// } from "~/hooks/useList"
// import { createTRPCRouter, privateProcedure } from "~/server/api/trpc"
// import { Logger } from "~/utils/logger"
// import { newListSchema, renameListSchema } from "~/utils/schemas/listSchemas"
// import { clerkClient } from "@clerk/nextjs/server"

// export const listsRouter = createTRPCRouter({
//   // inviteFriendToList: privateProcedure
//   //   .input(z.object({ email: z.string() }))
//   //   .query(async ({ ctx, input }) => {
//   //   const user = clerkClient.users.
//   //     await ctx.prisma.notification.create({data: {code: "INVITATION", issuer: ctx.userId, userId: }})
//   //   }),
// })
export {}
